<!-- -*- mode: XML -*- -->
<!-- run velodyne_pointcloud/CloudNodelet in a nodelet manager for an VLP-16

arg: calibration = path to calibration file (default: standard VLP16db.yaml)
pcap = path to packet capture file (default: use real device)

$Id$
-->

<launch>
  <group ns="lidar1" >
    <!-- declare arguments with default values -->
    <arg name="parent_frame_id" default="velodyne" />
    <arg name="frame_id" default="velodyne_fl" />
    <arg name="pcap" default="" />
    <arg name="manager" default="$(arg frame_id)_nodelet_manager" />
    <arg name="device_ip" default="192.168.1.201" />
    <arg name="port" default="2369" />
    <arg name="calibration" default="$(find velodyne_pointcloud)/params/VLP16db_fl.yaml"/>
    <arg name="min_range" default="0.4" />
    <arg name="max_range" default="130.0" />
    <arg name="model" default="VLP16"/>
    <arg name="topic_name" default="points_raw"/>

  
    <!-- start nodelet manager and driver nodelets -->
    <include file="$(find velodyne_driver)/launch/nodelet_manager.launch">
      <arg name="frame_id" value="$(arg frame_id)" />
      <arg name="device_ip" value="$(arg device_ip)" />
      <arg name="port" value="$(arg port)" />
      <arg name="model" value="$(arg model)"/>
      <arg name="pcap" value="$(arg pcap)"/>
    </include>

    <!-- start cloud nodelet -->
    <node pkg="nodelet" type="nodelet" name="cloud_nodelet"
          args="load velodyne_pointcloud/CloudNodelet $(arg manager)">
      <param name="calibration" value="$(arg calibration)"/>
      <param name="min_range" value="$(arg min_range)"/>
      <param name="max_range" value="$(arg max_range)"/>
      <remap from="velodyne_points" to="$(arg topic_name)"/>
    </node>
    <!-- static tf -->
    <node pkg="tf" type="static_transform_publisher" name="localizer_rc_to_localizer_fl" args="0.960457  -0.280691 0.00677087  3.12494 -3.12518  3.13387 $(arg parent_frame_id) $(arg frame_id) 10" />
    <!--<node pkg="tf" type="static_transform_publisher" name="localizer_rc_to_localizer_fl" args="12 -1.275 1 0 0 0 $(arg parent_frame_id) $(arg frame_id) 10" />-->
  </group>  
</launch>
